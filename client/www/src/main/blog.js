//import {FORM_CHANGE, POST_ORDER, RECIEVE_ORDERS, ORDER_POSTED} from './actions';

function blog(state = {
}, action) {
    switch(action.type) {
        default:
            return state;
    }
}

export default blog;
